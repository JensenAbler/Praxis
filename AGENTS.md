# Praxis

The current authorized milestone extends the proven phone coding/publication/deployment workflow in four areas: new projects, production diagnosis and recovery, ordinary dependency changes, and independent Praxis self-improvement. Implement working capabilities, qualify them through authenticated tools, and preserve recovery across conversations. The existing probe remains a regression fixture.

- Keep the probe's built-in heartbeat tools bounded. General coding commands belong only in a separate development sandbox without production secrets or host administration privileges.
- Keep OAuth credentials and runtime state outside Git and outside ordinary development access.
- Preserve Apocrypha, VPS Observer, podcast-production, and unrelated services/source revisions. Isolated Praxis coding services, protected Git publication configuration, fixed podcast-discord service adoption, disposable fixtures, and validated graceful nginx reloads are within the owner's bootstrap authority.
- The owner explicitly authorizes Praxis to commit and push to `main` by default, create new projects, diagnose/recover registered production deployments, support dependency updates, and independently update its application. Use protected adapters with scoped credentials; ordinary commands must never receive production credentials or host administration rights. Preserve exact revision preconditions and uncertain outcomes without blind repetition. Do not expose arbitrary privileged commands or success-returning stubs.
- Self-update must leave authentication authority, installed updater, credentials, protected adapters and host policy outside candidate application control. Stage candidate code without live state/leases or production rights, retain a working release, and recover failed/interrupted activation through the independent updater. Permission expansion and protected-component replacement still require the owner-controlled path.
- Package downloads belong behind an explicit sandbox network policy. Builds and dependency scripts execute only without production secrets or privileged access. Deployment binds source and dependency evidence to exact digests.
- Compare a small historical coding task with independent behavioral checks; keep expected solutions out of the worker's task context. Distinguish automated client evaluation from an actual phone run.
- Keep jobs independent of MCP requests and application restarts. Never rerun an ambiguously completed job.
- Run `npm test` and exercise authenticated MCP calls before deploying.
- Record actual evidence and distinguish native-client, API-client, fixture, and production results.
- The owner now requests a public Praxis GitHub repository. Commit and push reviewable increments as work proceeds. This supersedes the original specification's private-repository default; keep credentials, private runtime data, and other repositories' source out of publication.
- The owner requests the canonical public endpoint `https://mcp.jensenabler.com/praxis/mcp` and `/praxis/oauth` issuer. This supersedes the earlier stable `/praxis-probe` URL assumption. Preserve stored jobs, workspaces, credentials, and unrelated services during migration.
